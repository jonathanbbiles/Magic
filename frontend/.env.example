# Use your Render backend URL (MUST be https on iOS Expo Go)
EXPO_PUBLIC_BACKEND_URL=https://magic-lw8t.onrender.com

# Optional: only needed if your backend sets API_TOKEN
EXPO_PUBLIC_API_TOKEN=

# After changing values: restart with `npx expo start --clear` and fully reload the app.
