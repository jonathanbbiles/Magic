const {
  canonicalPair,
  canonicalAsset,
  normalizePair,
  toAlpacaSymbol,
  alpacaSymbol,
  normalizeSymbolInternal,
  normalizeSymbolForAlpaca,
  isCrypto,
  SUPPORTED_CRYPTO_QUOTES,
} = require('../shared/symbols');

module.exports = {
  canonicalPair,
  canonicalAsset,
  normalizePair,
  toAlpacaSymbol,
  alpacaSymbol,
  normalizeSymbolInternal,
  normalizeSymbolForAlpaca,
  isCrypto,
  SUPPORTED_CRYPTO_QUOTES,
};
